name: 'dbt_ons'
version: '1.0.0'


profile: 'dbt_ons'

model-paths: ["dbt_ons/models"]
analysis-paths: ["dbt_ons/analyses"]
test-paths: ["dbt_ons/tests"]
seed-paths: ["dbt_ons/seeds"]
macro-paths: ["dbt_ons/macros"]
snapshot-paths: ["dbt_ons/snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_ons:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
